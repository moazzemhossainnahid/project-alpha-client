import React from 'react';
import { useParams } from 'react-router-dom';

const Service = () => {
    const {id} = useParams();
    
    return (
        <div>
            
        </div>
    );
};

export default Service;