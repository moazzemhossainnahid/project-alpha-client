import React from 'react';
import Navbar from './Navbar';

const Header = () => {
    return (
        <div className='sticky-top'>
            <Navbar/>
        </div>
    );
};

export default Header;